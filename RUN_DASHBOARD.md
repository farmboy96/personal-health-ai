# Running the Health Dashboard

From the repo root:

```
backend\.venv\Scripts\streamlit.exe run dashboard.py
```

Then open http://localhost:8501 in a browser.

The app runs locally only — nothing is exposed to the network.
